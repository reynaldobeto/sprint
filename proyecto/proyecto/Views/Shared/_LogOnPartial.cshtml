@if(Request.IsAuthenticated) {
    <text><b>Bienvenido</b>&nbsp <strong>@User.Identity.Name</strong>!
    @Html.ActionLink("Salir", "LogOff", "Account") </text>
}
else {
     @Html.ActionLink("Login..", "LogOn", "Account") 
}
